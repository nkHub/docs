# 后端

```
    mac服务器ssh连接
    登录：ssh root@serverIP -p 端口号
    退出：control + d

    <!-- 暴露命令至全局 -->
    ln -s command-location /usr/local/bin/
```

```
    whereis nginx.conf
    cd 路径/替换证书文件
    nginx -s reload // 重启nginx
```